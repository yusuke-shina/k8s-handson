# kind-config.yaml（クラスタ作成時に使用）
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080
        hostPort: 30080        # ホストの30080 → kindの30080
